version: 0.2

phases:
  pre_build:
   commands:
       - echo "creating <path-to-folder> folder"
       - mkdir -p ./hello-world/
  build:
    commands:
        - echo "Copying the file"
        - cp hello-world.html ./hello-world/
artifacts:
   files:
      - hello-world/**/*
 
